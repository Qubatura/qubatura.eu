# Session handoff — 2026-07-07 (qubatura.eu) — czytać PIERWSZE po /clear

Kontynuacja po SESSION-2026-07-06-pm.md. **STRONA JEST LIVE NA https://qubatura.eu** (własna domena, IQ Host). Auto-deploy działa. Zostały: **tło-wideo home** (czeka na plik) + **drobne poprawki merytoryki**. Premiera FB — cel piątek. ⚠️ Limit tygodniowy Kuby ~85% — oszczędzać tokeny, robić hurtem.

## 🟢 STAN: LIVE + auto-deploy
- **Branch `dev`. HEAD:** `e9d7551`. Working tree czysty.
- **qubatura.eu = live** (serwuje z roota domeny). Weryfikacja: `curl -I https://qubatura.eu/` → 200; `https://qubatura.eu/send.php` → 405 (POST-only, poprawnie).
- **Podgląd też na Pages:** `https://qubatura.github.io/qubatura.eu/src/` (dev, /src/ struktura).
- **Token cache:** `mrab36sg`. ⚠️ `node tools/stamp-version.mjs` PRZED każdym pushem (JS/CSS cache-bust).

## 🚀 JAK DZIAŁA DEPLOY (kluczowe!)
**`git push` na `dev` → GitHub Action (`.github/workflows/deploy.yml`) → build + FTP → live w ~1.5 min.** Nic ręcznie.
- `tools/build-prod.mjs` — robi `dist/` serwowalny z ROOTA domeny: płaska kopia `src/*` + `assets/` do `dist/assets/`, i przepisanie ścieżek: root-HTML/JS `../assets/`→`assets/`, CSS `../../assets/`→`../assets/`. (Dev/`src/` NIE ruszany — Pages dalej pod `/src/`.) `dist/` jest w `.gitignore`.
- **Sekrety GitHub** (ustawione): `FTP_SERVER`=host91083.iqhs.pl, `FTP_USERNAME`=deploy@qubatura.eu, `FTP_PASSWORD`, `FTP_SERVER_DIR`=`./` (konto FTP per-domena → ląduje w public_html; NIE pełna ścieżka!).
- **Podgląd runa:** `gh run list --repo Qubatura/qubatura.eu --workflow deploy.yml`; `gh run watch <id> --repo Qubatura/qubatura.eu --exit-status`.
- **Formularz:** `src/send.php` (PHP 7.4, mail na biuro@qubatura.eu, honeypot). `contact-console.js` POST-uje na `send.php`; poza domeną (localhost/Pages) `IS_LIVE=false` → tryb podglądu (showSuccess bez wysyłki). ⚠️ **DO TESTU: czy mail dochodzi (też SPAM)** — jak nie, przełączyć na SMTP.
- Sprzątanie na serwerze (Kuba, menedżer plików IQ Host): usunąć śmieć `public_html/domains/` (z 1. błędnego deployu). NIE ruszać `.ftp-deploy-sync-state.json` ani naszych plików.

## ✅ LISTA KUBY 07-07 — ZROBIONE (HEAD `c293f21`, live)
- **#1 tło-wideo** ✓ `assets/planet-bg-web.mp4` (1600px, crf30, faststart, **280KB**). `scene.js`: desktop=`THREE.VideoTexture`, mobile=webp (autoplay/perf), pauza gdy karta w tle, webp-fallback. Master 4.3MB → `_source/` (gitignore, poza deployem). ⚠️ Kuba ocenia na Macu czy nie muli → jak muli: rewert scene.js do TextureLoader webp.
- **#2 chipy multi-select** ✓ `contact-console.js`: `toggleChip`+`composeAndAdvance`, chip `.on` (✓+kolor działu), przycisk `#cc-next` DALEJ (aktywny gdy ≥1). Temat=tytuły przez „ + ", opis=szkielety pól sklejone. HTML krok2 + CSS `.cc-chip.on`/`.cc-next`.
- **#3 colophon** ✓ przebudowany: plakietka frost Z DOŁU (`.cph-plate` translateY), linia SVG `pathLength=1` sama się rysuje, teksty `[data-decode]` scramble→treść (`colophon.js scramble()`), fireflies canvas. Copy „Zero gotowca."
- **#4 Lab hero** ✓ lead wyostrzony pod software (delikatny lifting, H1 bez zmian).
- **#5** — Kuba podmieni 2 mockupy w Lab SAM.

## 🎨 COLOPHON — iteracje 07-07 pm (HEAD `bb35980`, live)
Plakietka `#colophon` (klik tagline „Przybywamy z sygnałem") przeszła kilka rund z Kubą:
- **Odrzucone:** „Zero gotowca" (za krzykliwe), primary pasek/SVG, scramble-kod, zaokrąglone rogi.
- **Stan obecny (zaakceptowany kierunek):** prosty **frost 1:1 z `.pl-card`** (OSTRE rogi, gradient 135° + border 22% + poświata obrysu + blur 6px, `--pg-color:var(--color-primary)` na `.cph-plate`), wjeżdża Z DOŁU. Nagłówek spokojny **„Technologia tej strony"**. Efekty CSS: `.cph-sheen` (przejazd światła po szkle raz) + treść wyłania się z szyby fadem (`[data-reveal]` blur→ostrość, stagger). Fireflies subtelne (`colophon.js`).
- **Opis (brief technicznie):** „Postawiona na czystym JavaScripcie — vanilla, ES modules… Three.js, shadery, GSAP, Web Audio API, SPA History API. Zero WordPressa, zero gotowych modułów — od pierwszej linijki. **Full custom.**"
- **Kredyt 2 linie:** „Projekt, kod, budowa — zespół Qubatura Lab" / „Web design, muzyka — Kuba Hyrja · CEO Qubatura Group".
- **Guzik** „Opisz projekt" = `.page-cta` pełna szerokość + napis na środku. **„Wróć" = lewy górny** plakietki.
- **`<title>`** skrócony → „Qubatura — Eventy · Studio · Web design · Śląsk".
- **Wideo tła** `playbackRate 0.8` (scene.js).
- ⚠️ Kuba może zgłosić: opis-brief vs tagi pod nim trochę się dublują (opcja: okroić tagi/opis).

## ⏳ TERAZ (07-07 pm, przed premierą FB **18:00**): Kuba testuje poprawki na Macu → wraca z feedbackiem + **spisaną listą poprawek MOBILE** (po zmianach 07-07 mobilka się rozjechała — do ogarnięcia hurtem). Cel: dopieścić max przed 18:00. ⚠️ Kuba blisko limitu tygodniowego — oszczędzać tokeny, batch.

## ⏭️ (poprzednia lista — dla kontekstu)
1. **Tło-wideo home** — plik JEST: `assets/planet-bg.mp4` **3324×2492, 4,3 MB, h264, 6s, 24fps** → ⚠️ **PRZEKODOWAĆ NA LEKKI** (ffmpeg, ~1600px szer., niższy bitrate, faststart; cel <1 MB; lekkość priorytet). Tło home to **tekstura WebGL** (`scene.js` ~l.47: `TextureLoader('../assets/planet-bg.webp')`, `MeshBasicMaterial` opacity 0.17 desktop / 0.26 mobile, `planetMesh` z parallaxem). Podmienić na **`THREE.VideoTexture`** (materiał/opacity/pozycja BEZ zmian → 1:1). **Desktop = wideo; mobile = webp** (perf+autoplay). Pauza gdy karta w tle; webp fallback przy błędzie. **Jak muli → rewert** (test „wisienka", Kuba ocenia na Macu).
2. **Formularz — chipy podpowiedzi = TOGGLE-SELECT.** Teraz `.cc-chip` (contact-console.js `pickItem`) klik → wpisuje szablon w textarea + skok do kroku 3. Kuba chce: chipy **klikalne do zaznaczenia (podświetlenie), multi-select** — klient tylko zaznacza (nie pisze), a zaznaczone **komponują wiadomość/temat** (żeby Kuba widział co wybrał). **Dotyczy KAŻDEGO pola wyboru, w KAŻDYM dziale i na KAŻDYM etapie** formularza.
3. **Colophon redesign** (`#colophon` z taglinu) — teraz zwykły overlay. Kuba chce: **tabliczka wyjeżdżająca Z DOŁU na home** (frost-szkiełko NASZE, MNIEJSZA niż teraz), z eleganckim ale **technologicznym bajerem** (coś się rysuje na froscie delikatnie → tekst pojawia się z rozmazania/blur; można użyć robaczków/fireflies). **Copy (do przeredagowania):** „strona zbudowana na [parallax · vanilla JS · Three.js · custom shadery], **zero szablonu, zero WordPressa**, grafiki/efekty/kolory **customowe, zero gotowca**". **3 POMYSŁY EFEKTU (Kuba wybierze):** A) samo-rysująca się cienka linia na froscie (SVG stroke) → tekst z blur→sharp + robaczki dryfują; B) „sygnał się stroi" — waveform/EQ przebiega po froscie → tekst wyłania się z szumu (spina z audio-brandem); C) „kod się kompiluje" — mono-tagi tech pojawiają się jak build-steps ✓, potem nagłówek z blur (mocny flex pod rekrutera/ElevenLabs). **✅ WYBRANE PRZEZ KUBĘ: A + scramble-decode.** Plakietka wyjeżdża Z DOŁU (frost, mniejsza), cienka fioletowa linia **sama się rysuje** po froscie, a teksty (nagłówek + copy) **DEKODUJĄ się z losowych znaków/ASCII w ~2s** — litery lecą i po kolei wskakują na miejsce → wyłania się realna treść (flex „my to kodujemy"). Robaczki/fireflies dryfują po tafli.
4. **Lab hero — przeredagować opis pod „software"** (do przegadania z Kubą).
5. Kuba **podmieni 2 mockupy w Lab** (zrobi sam, później).
6. Po tych: **na dziś koniec ze stroną.**

## ✅ ZROBIONE (sesje 07-06/07 — skrót)
- **Karta produktu Qplayer** (`#product-overlay`, klasy `pc-*`, 3/4, 6 funkcji, spec-sheet, Mac-first). Kafle Qplayer/Qtune symetryczne.
- **Showcase „na zamówienie"** (`#lab-showcase`, guzik `.page-showcase` w hero Lab): tło Lab webp, 3 filary, **latająca galeria** kart platform (miniaturki realnych mockupów, `assets/lab/mockupy/thumbs/0X.webp`) → **detal `#lab-case`** (iframe pełnego mockupu + ‹ ›). 5 mockupów HTML w `assets/lab/mockupy/` (TORQ/Młode Orły/Atelier/IRON LAB/Stół.).
- **Colophon** (`#colophon`, klik taglinu „Przybywamy z sygnałem"): „Pod maską." + stack techniczny + kredyt „zespół Qubatura Lab · Kuba Hyrja, CEO". `colophon.js`.
- **Deep-link CTA Lab** → formularz z torem Lab (`data-contact-tor`).
- **Mini-player:** magenta→primary, puls-zaproszenie mocny (rozbłysk). **Cookie:** ostre krawędzie + puls primary, tekst Chakra Petch.
- **Deploy:** send.php + build-prod + Action + LIVE.

## 📋 PREMIERA — zostało (patrz PREMIERA-CHECKLIST.md)
- 🟠 **TEST formularza na żywo** (mail dochodzi? spam?) — jak nie, SMTP.
- 🟠 **favicon** — wpiąć `favicon-dark.svg` (index.html dalej na `favicon.svg`).
- 🟠 **og-image** pod domeną (sprawdzić czy `qubatura.eu/assets/og-image.png` nie 404 — ważne pod share na LinkedIn/FB).
- 🟡 teksty (zbiorcza lista Kuby), sprzątanie `public_html/domains/`.
- 🎯 pod ElevenLabs (aplikacja Kuby): dopiąć Studio + Qplayer.

## Konwencje
PL. Vanilla JS, dark, premium, ZERO generycznych AI aesthetics (Kuba pilnuje). Show-before-commit (poligon + headless-screenshot: świeży `--user-data-dir`, `--no-sandbox`, powtórz po „NOFILE"; między strzałami `ping -n 3 127.0.0.1` bo `sleep` zablokowany; iframe → `--virtual-time-budget=4000`). `stamp-version.mjs` przed pushem. Funkcje-treść działają u KAŻDEGO (nie bramkować za prefers-reduced-motion). Workflow: **edit → push → live ~1.5 min.**

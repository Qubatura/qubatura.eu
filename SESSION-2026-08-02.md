# Session handoff — qubatura.eu — 2026-08-02 — CZYTAĆ PIERWSZE po /clear

Poprzedni handoff: `SESSION-2026-07-15.md`. Między 07-15 a dziś **nie było notatek** — trzy tygodnie
pracy (bramka pobierania Qplayera, karta produktu, sekcja silników) siedzą wyłącznie w historii gita.
Jak szukasz stanu tamtych rzeczy: `git log --oneline` od `543d99f` w dół.

## 🟢 Stan ogólny
- Strona LIVE, branch `dev`, auto-deploy przez GitHub Action + FTP (~1,5 min od pusha).
- **PRZED każdym pushem gdy ruszasz `src/`:** `node tools/stamp-version.mjs`.
- Na starcie dnia były **2 niewypchnięte commity** z bramką pobierania 0.66.5/0.66.7 — wypchnięte.

## ✅ Zrobione dzisiaj

### 1. Mockupy Lab — koniec „wyglądu AI"
Kuba: „random, restauracja nazywa się Stolik, wygląda jak szablon AI".
- **Nagłówek „Qubatura Lab — mockup platformy / branża N z 5" skasowany** — każda makieta sama się
  przedstawiała jako ćwiczenie. W to miejsce kontekst marki (branża + adres).
- **Nazwy nudne i konkretne:** Auto-Serwis Wójcik, UKS Iskra, Nails by Marta, Atlas Gym, Zielona 27.
- **Każdy plik ma własny układ** — to było najgłośniejsze „jeden generator" (wszystkie miały
  telefon+telefon+desktop w rzędzie).
- **Emoji jako ikony → SVG** (skill `ui-ux-pro-max`, reguła no-emoji-icons).
- 03 i 05 miały **identyczną parę fontów** — restauracja dostała Playfair Display SC + Karla.
- Brud w danych: ceny nierówne, przecinek dziesiętny po polsku, ogonki w nazwiskach.
- **`tools/zrzut-mockupy.cjs`** — miniatury robi Electron (pożyczony z `Qplayer/node_modules`),
  bo headless Chrome na MSI nie działa od 07-14. PIL przycina do proporcji karty i zapisuje webp.

### 2. Panel strojenia sygnetu — `?tune=1`
`src/js/tune-signet.js`. Ładuje się **wyłącznie** pod `?tune=1` (import dynamiczny → gość bez
parametru nie pobiera pliku). 13 suwaków + barwa rantu + zapis w `localStorage` + „Kopiuj wartości"
(gotowy blok do wklejenia). Presety „desktop (dziś)" / „mobile (dziś)".
- ⚠️ **`localStorage` wygrywa z kodem.** Jak wejdziesz z `?tune=1` i zobaczysz stare wartości, to
  są twoje zapisane — kliknij **„Cofnij do kodu"**.
- Barwienie w stronę działów (`uDivColor`/`uDivMix`) panel **nie rusza**.

### 3. Poligon profilu bryły — `poligon/sygnet-zaokraglenie.html`
Suwaki bevel/głębia/segmenty na **tej samej geometrii i skali co `signet.js`**. Materiał zastępczy
(`MeshPhysicalMaterial` + `RoomEnvironment`) — **tylko o kształcie**, nie o nastroju.
- Guzik **„Obrys wzorcowy"** — kontur przed zaokrągleniem. Odjazd bryły od pomarańczowej linii =
  bevel wszedł sam w siebie.
- ⚠️ Wymaga serwera: `python -m http.server 8778` w katalogu repo →
  `http://localhost:8778/poligon/sygnet-zaokraglenie.html`.
- **Naprawiony błąd:** suwak głębi był w surowych jednostkach SVG, a podpisany „na stronie: 8".
  `signet.js` trzyma głębię jako `X / S` (jednostki świata). Teraz suwaki mówią językiem kodu.

### 4. Sygnet — materiał, geometria, ruch

**Mapa otoczenia (`zbudujOtoczenie()`).** Diagnoza dnia: shader liczył refrakcję i fresnela, ale
bryła **nie miała czego odbić** — scena nie ma żadnego otoczenia. Stąd „neon zamiast szkła":
brakowało nie substancji w środku, tylko świata na zewnątrz. Panorama równoprostokątna malowana
na canvasie (smuga „wieży", poświata „planety", ciemny dół) — zero plików do pobrania.
Odbicie dokładane **PO** korekcie odcienia, żeby `uPrimaryShift` nie ściągnął mu czerwieni.

**Koniec rozjazdu mobile ↔ desktop** (założenie Kuby: „nastawy takie same"). W uniformach **nie ma
już ani jednego `isMobile`**. `isMobile` zostaje tylko przy `SIZE_MUL` (sygnet +20% na telefonie).

**Ruch — „dostojny, nie festynowy".** Kuba trafnie połączył kropki: „za gruby… chyba że tu jest pies
pogrzebany, że on się źle kręci". Grubość i obrót to jeden problem — przy ±29° głębsza bryła
pokazywała całą ściankę boczną. **Pełnego obrotu świadomie nie robimy**: w połowie widać znak od tyłu
(lustrzane Q = czyta się jak błąd), a przy 90° bryła zamienia się w kreskę.
Zamiast tego: **znak prawie stoi, a panorama dryfuje wokół niego** (pełny obieg ~21 min) → refleks
przejeżdża po szkle. Drogie strony nie kręcą logotypem, tylko prowadzą po nim światło.

## 📐 Aktualne nastawy sygnetu (jeden reżim, obie platformy)

| parametr | było (desktop/mobile) | jest |
|---|---|---|
| głębia | 7.5 | **11.0** |
| bevelSize (bok) | 0.25 | **0.40** — zmierzony próg, od ~0.55 ogon Q puchnie |
| bevelThickness (w głąb) | 0.25 | **2.20** — ogona NIE dotyka, zabija ostry kant |
| segmenty | 4 / 32 | **14 / 80** (~339 tys. trójkątów) |
| `uFillDensity` | 0.82 / 0.28 | **0.42** |
| `uOpal` | 1.8 / 2.4 | **2.2** |
| `uFrontFlat` | 1.0 / 0.0 | **0.0** |
| `uFrontClear` | 0.0 / 1.0 | **1.0** |
| `uEnvIntensity` | — | **0.85** |
| kołysanie Y | 0.51 rad (~29°) | **0.24 rad (~14°)**, czas ×0.85 |

**Kluczowe odkrycie techniczne:** `ExtrudeGeometry` ma **dwa niezależne** zaokrąglenia. `bevelSize`
odsuwa krawędź na boki i to on niszczy ogon Q (próg 0.40). `bevelThickness` zaokrągla w głąb i
ogona nie dotyka wcale. Wcześniej były równe, więc limit ogona trzymał w dole całą obłość.

## 🎯 Do zrobienia dalej

1. **Ocena Kuby po powrocie** — nastawy powyżej to MOJA propozycja („drogo a skromnie"), nie jego
   wybór. Poligon trzymamy pod ręką, wraca do zabawy.
2. **Poświata wokół sygnetu (`GLOW_GAIN`, glow plane) — NIETKNIĘTA.** Moim zdaniem to ona najmocniej
   ciągnie wygląd w stronę „neon" i robi robotę, którą powinno robić szkło. Kandydat numer jeden
   do ściszenia — ale to zmiana obecności marki, więc czekam na zgodę.
3. **Panorama otoczenia może być za uboga.** Jeśli po podbiciu `uEnvIntensity` do 1.2–1.5 dopiero
   robi się dobrze, to znaczy że trzeba dołożyć jasnych źródeł w `zbudujOtoczenie()`, a nie kręcić suwakiem.
4. **Wydajność na telefonie** — 339 tys. trójkątów zamiast 143 tys. (skutek ujednolicenia). Gdyby
   telefon się ciął: ciąć `bevelSegments`/`curveSegments` do 8/56 — gęstość siatki nie zmienia wyglądu.
5. **Prawdziwa rurka = rebranding.** `signet.svg` to jedna ścieżka z wypełnieniem, bez linii środkowej
   → `TubeGeometry` niewykonalne. Przy modernizacji logo (przyszły rok) zamawiać znak **jako krzywe**.
6. **Silnik na zewnątrz** — Kuba chce jednoplikowe narzędzie: wrzucasz SVG → bryła → suwaki materiału,
   ruchu, koloru → eksport. Wycena: ~60% to przeniesienie tego, co już mamy; koszt siedzi w
   wczytywaniu pliku i w **tle** (refrakcja musi mieć co zaginać). Jedna skupiona sesja na v1.
7. **Jedna sprawa na stronie** — Kuba zapowiedział, że napisze „z drogi". Nieznana.
8. **Sansit** — następny projekt, „jak przyjadę". Osobny folder, osobny handoff.

## 🗂️ Zaległości z 07-15 (nadal otwarte)
Weryfikacja EN przez native'a (tagline „We arrive with the signal", cytaty Bowie/Marconi),
Awwwards — rejestracja, SEO/`hreflang`, audyt mobile on-device, a11y, lżejsze audio.

## 📁 Pliki dotknięte dzisiaj
`src/js/signet.js` (geometria, mapa otoczenia, ruch, ujednolicenie), `src/js/tune-signet.js` (nowy),
`src/js/main.js` (import pod `?tune=1`), `src/js/lab.js` (nazwy platform),
`assets/lab/mockupy/*.html` + `thumbs/*.webp`, `poligon/sygnet-zaokraglenie.html` (nowy),
`tools/zrzut-mockupy.cjs` (nowy).

## ⚠️ Lekcje warsztatowe
1. **Electron zastępuje headless Chrome** — `Qplayer/node_modules/electron/dist/electron.exe`
   + skrypt `.cjs`: `loadURL`, `executeJavaScript`, `capturePage`. Działa na MSI, robi zrzuty,
   czyta konsolę. Okno **musi być widoczne** (`show: true`) — na ukrytym `capturePage` daje czerń.
   `app.disableHardwareAcceleration()` dla stabilności.
2. **Sprawdzaj to, o co pytasz.** Przy bevelu sprawdziłem, czy ogon *zniknął* (nie zniknął) i uznałem
   próg za wyższy. Kuba zobaczył, że ogon się *wykrzywił*. Pomiar musi dotykać warunku.
3. **Jednostki suwaków muszą być jednostkami kodu.** Podpisałem suwak „na stronie: 8", gdy kod trzymał
   `7.5 / S` — liczby od Kuby nie znaczyły tego, co myślał.
4. Regexowy `grep -o "x.js?v=[a-z]*"` kłamie — `?` to kwantyfikator. Do sprawdzania stringów `grep -F`.

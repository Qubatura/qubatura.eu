# Session handoff — 2026-07-05 (qubatura.eu) — Kontakt/formularz + RODO + cookies + Web3Forms scaffold

Ostatnia prosta przed premierą. Duża sesja: przebudowa formularza Kontakt + domknięcie „nie-design" rzeczy do premiery. **Wszystko na `dev`, NIC niezacommitowane** (working tree dirty) — Kuba robi finalny przegląd wieczorem (~18) i wtedy commit/push.

## Stan repo
- **Branch:** `dev`. **HEAD:** `88d627a` (bez zmian dziś — wszystko w working tree).
- **Serwer:** `python -m http.server 8080` z roota → `http://localhost:8080/src/`.
- **Token cache:** `mr6t2en6`. ⚠️ **Przed pushem:** `node tools/stamp-version.mjs` — dodany NOWY moduł `cookie-consent.js` (import w main.js z `?v=mr6t2en6`); potwierdzić że stamp go stempluje.

## ⏭️ CO CZEKA NA KUBĘ (po 18)
1. **Klucz Web3Forms** → wklej do `WEB3FORMS_KEY` (góra `contact-console.js`). Pusty = tryb podglądu (pokazuje sukces, NIE wysyła). Po wklejeniu → testowy send + sprawdzić czy mail czysty (bez stopki „powered by"). Jak stopka przeszkodzi → plan B: Cloudflare + Resend (własna domena).
2. **Adres do polityki** — w `src/polityka-prywatnosci.html` zostały placeholdery `⟨ulica i numer⟩` + `⟨kod pocztowy⟩` (reszta wpisana: Qubatura Jakub Hyrja, NIP 6443374174, data 5 lipca 2026).
3. **Wybór `<title>`** — patrz sekcja SEO niżej (przygotowane alternatywy; Kuba obecny „Events · Studio · Lab" chce zmienić).
4. **Lab — treść** (pomysł Kuby): 2 produkty-kafelki — Event Player (opis + 1–2 screeny + guzik „Pobierz — wkrótce" = gotowy) + drugi „w produkcji · premiera IX/X". Grafiki 2/departament = materiał na te podstrony (są w zanadrzu, NIE podmieniać teł).
5. **Sekretny eksperyment na home** — Kuba coś próbuje, nie zdradził.
6. **3. utwór playera** — wrzucić plik dokładnie jako `assets/audio/Qubatura.eu-relax.mp3` (slot 3 „Relax" już wpięty, zagra sam).
7. Finalny przegląd → **commit/push → premiera**.

## Zrobione dziś (chronologicznie)

### 1. Formularz Kontakt — pełna przebudowa (zaakceptowane przez Kubę „jest super")
- **Szkiełka frost 1:1 z playerami** (`.pl-card`): brama `#cc-gate` + kafle `.cc-tor` + panel `.cc-wrap` → **ostre rogi 0px** (precz „obłości"), prawdziwy `backdrop-blur`, prawie zero fillu (scena prześwituje), obwódka w kolorze działu. Brama mniejsza, bez `◗◗◗` na górze.
- **Wskaźnik toru:** waveform → **oddychająca obręcz** `.cc-ring` (rym z `.pl-ring`/sygnetem, tylko puls, zero rotacji — „nie festyn"). Wybór z poligonu `poligon/wskaznik-obrecz.html`.
- **Nagłówek konsolety:** „CENTRUM STEROWANIA KOMUNIKACJĄ" → **„CENTRUM ŁĄCZNOŚCI"** (kicker przy pytaniu). Górny rząd utility: **„‹ Wstecz" po lewej** (styl `.prod-close`, bez ramki, strzałka z galerii) + **licznik `01 / 03`** po prawej (segmenty precz).
- **Teksty:** pytanie „Z czym przychodzisz?" → **„Jaką masz sprawę?"**; sub → **„Wybierz temat — dalej poprowadzimy."**; sub bramy → „otwórz centrum łączności →".
- **Panel** bardziej przezroczysty (fill .42/.5). Pliki: `index.html`, `main.css` (blok `/* KONTAKT */`), `contact-console.js`.

### 2. Branding — reguła nazewnictwa (ustalona z Kubą)
- **Departamenty organicznie: Events / Studio / Lab** — Q fizycznie NIE występuje w nazwie, „nasuwa się z sygnetu". Cofnięte „Qlab", kafle toru = EVENTS/STUDIO/LAB, „STUDIO Q" znika.
- Pełna forma w świat = „Qubatura + departament". „Qlab" tylko luźny skrót w mowie.
- **Opis Lab** = nacisk www/UX/aplikacje: „Strony www, aplikacje, UX design, IoT, pomiary akustyczne" (druk 3D OUT — bardziej eventowe/Pan Topperek; pomiary IN — realna robota). Podpowiedzi Lab: www / aplikacja / UX / IoT / pomiary / luźny pomysł.

### 3. Kontrast (Kuba: było za ciemne)
- „KONTAKT / EN" + waveform + tagline „Przybywamy z sygnałem": `0.45 → 0.62` + text-shadow. **Player nietknięty** (celowo przygaszony).

### 4. Muzyka home (player.js) — pliki od Kuby
- Slot 1 **Theme** (`Qubatura.eu-theme.mp3`), slot 2 **Pulse** (`Qubatura.eu-pulse.mp3`), slot 3 **Relax** (`Qubatura.eu-relax.mp3` — plik dojdzie).
- **Autoplay: NIE** (świadomie). Przeglądarki blokują audio-autoplay + UX. Start w ciszy, gra po kliknięciu w waveform (gest odblokowuje audio). To rekomendacja i tak już zaszyta w player.js.

### 5. RODO
- **Checkbox** w kroku 3 (`.cc-rodo`, przed „WYŚLIJ SYGNAŁ") — wymagany, blokuje wysyłkę, stan błędu `.err`. Link „polityką prywatności" (SPA-proof przez `[data-policy]`).
- **Honeypot** `.cc-hp` (ukryte pole, poza ekranem) — bot wypełnia → wysyłkę cicho porzucamy. Przekazywany też jako `botcheck` do Web3Forms.
- **Nowa podstrona `src/polityka-prywatnosci.html`** — brandowa (Chakra Petch + Space Mono, ciemna), sekcje RODO PL + cookies. Placeholdery na adres. Linkowana z checkboxa + stopki Kontaktu (`.contact-policy`).
- Uwaga: nie-prawnik. Standardowy szablon dla formularza kontaktowego — warto dać komuś do rzutu okiem.

### 6. Cookie consent (nowy `src/js/cookie-consent.js`)
- Minimalny baner „tylko niezbędne" (dół ekranu, frost), „Rozumiem" → zapamiętane w `localStorage` (`qub-cookie-ok`). Link do polityki. Wpięty w `main.js` (`initCookies()`). Style w `main.css` (`.cookie-bar`).

### 7. Web3Forms — scaffold wysyłki (`contact-console.js`)
- Stała `WEB3FORMS_KEY` (pusta). `send()` async: walidacja honeypot+RODO → jeśli brak klucza = pokaż sukces (podgląd) → jeśli klucz jest = POST JSON na `api.web3forms.com/submit` (subject = `[DZIAŁ] …`, name, kontakt, message, botcheck). Stan „WYSYŁAM…" + błąd inline (`.cc-status`). Sukces = ekran „Sygnał odebrany".

### 8. SEO
- `sitemap.xml` + wpis `polityka-prywatnosci.html`.
- JSON-LD wzbogacony: `legalName` „Qubatura Jakub Hyrja" + `vatID` „PL6443374174".
- **Alternatywy `<title>` do wyboru Kuby** (obecny „Qubatura Group — Events · Studio · Lab"):
  - A: „Qubatura — Events · Studio · Lab" (krócej, bez „Group")
  - B: „Qubatura — Nagłośnienie, produkcja audio, druk 3D · Sosnowiec" (keyword/lokalne SEO)
  - C: „Qubatura — Przybywamy z sygnałem" (brand)
  - D: „Qubatura Group — Nagłośnienie eventów, Studio audio, Lab" (opis + brand)
- **Do rozważenia (nie zmienione):** meta description ma jeszcze „(Studio Q)" — wg nowej reguły → „Qubatura Studio"; hasOfferCatalog „Druk 3D i projekty IoT" → można dołożyć www/aplikacje/UX. Zostawione Kubie (copy).

### QA
- `node --check` OK dla wszystkich zmienionych modułów. Home bootuje (loader+sygnet render), zero błędów JS w konsoli headless.

## ⚠️ Do potwierdzenia przed premierą (niezależne od dziś)
- **Domena qubatura.eu podpięta do Pages?** canonical/OG/sitemap wskazują `https://qubatura.eu/`, live = `qubatura.github.io/qubatura.eu/src/`. Inaczej og:image 404 + sitemap/canonical złe.
- **Routing SPA** (/events /studio /lab) na Pages wymaga 404.html→index żeby być indeksowalny.

## Konwencje
PL. Vanilla JS, dark, premium, bez generycznych AI aesthetics. Show-before-commit. Trudne decyzje wizualne → poligon + headless-screenshot. `stamp-version.mjs` przed każdym pushem JS/CSS.

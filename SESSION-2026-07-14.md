# Session handoff — qubatura.eu — 2026-07-13/14 — CZYTAĆ PIERWSZE po /clear

Kontynuacja po `SESSION-2026-07-13.md` (raport poranny Batch A). Ten plik jest teraz **najnowszym handoffem**.

## 🟢 Stan ogólny
- **Strona LIVE: https://qubatura.eu** — branch `dev`, **HEAD `5438bf6`**, wszystko zdeployowane i zweryfikowane.
- **Auto-deploy:** `git push origin dev` → GitHub Action → `build-prod.mjs` robi `dist/` serwowalny z roota → FTP wgrywa na IQ Host (~1.5 min).
- **PRZED każdym pushem gdy zmieniasz src:** `node tools/stamp-version.mjs` (cache-bust ?v= dla iOS — inaczej Safari nie pobierze nowych modułów/CSS).
- **Etap „design / pod Awwwards" ZAMKNIĘTY** decyzją Kuby („jest super, zamykam pod kątem awards"). Design (40%) i Creativity (20%) = mocny, dopięty materiał zgłoszeniowy.
- Untracked w repo (NIE commitować — materiał referencyjny Kuby): `Zrzut ekranu 2026-07-13*.png`, `synget desktop.png`, `synget mobile .PNG`.

## ✅ Zrobione w tej sesji (wszystko live)

### Batch A — „Lab Typography Pass" (rano 07-13, ZAAKCEPTOWANE)
CSS-only w `src/css/main.css`:
- Tagi tech `.cph-stack` → chip terminalowy (uppercase, letter-spacing .1em, 9px).
- Line-height opisów: `.page-lead` 1.95, `.cph-lead` 1.72.
- Bullety `.page-services` → ostry fioletowy kwadrat (marker przez `::before`, a11y ok).
- `::selection` w barwie marki; `:focus-visible` brand outline (dostępność klawiaturą); subtelny custom scrollbar.
- `.page-services` już było mono (potwierdzone). Kickery spójne — nie ruszane.
- Drobne: „Oświetlenie sceniczne (DMX)" → bez „(DMX)".

### Galeria Events — długa ewolucja, FINAŁ ZAAKCEPTOWANY („events jest super")
Przeszła: B&W „światło z ciemności" → **D→B** (realny kolor w spoczynku, hover=czysty) → **wtopienie w kosmos**. Ostateczny mechanizm:
- **Wychłodzenie zdjęcia w spoczynku**: `.gal-photo { filter: contrast(1.06) saturate(.55) brightness(.82) }` → zdjęcie przestaje być ciepłym prostokątem na zimnej fioletowej scenie.
- **Grade HUE do palety sceny**: `.gal-stage::after { mix-blend-mode: color; }` gradient cyjan (światła) → fiolet (cienie). Znika na hover.
- **Hover = żywy skok**: `.gal-photo` → `saturate(1.18) brightness(1.08)` + zoom.
- **Rozpuszczona krawędź (pomysł Kuby)**: `.gal-stage { inset: -6% }` + szeroki mask feather (radial `#000 32% → transparent 100%`) → zdjęcie „wylewa się" w scenę.
- **Atmosfera**: winieta + mgła dolna dryfująca (`.page-gallery::after`) + oddychająca poświata (`.page-gallery::before` `@keyframes galBreathe`).
- **Wyrównanie**: `.page-gallery { top: 19vh }` = góra zdjęcia na linii nagłówka hero.
- **Silnik** (`src/js/gallery.js`): jedno `<img class="gal-photo">` na slajd (nie ghost+glow — lżej). Zdjęcia `assets/events/ev-01..12.webp`.
- ⚠️ **LEKCJA**: scena jest już cała fioletowa → fioletowe overlaye / `soft-light` były NIEWIDOCZNE („fiolet na fiolecie"). Widoczny efekt daje dopiero wychłodzenie/desaturacja SAMEGO zdjęcia + `mix-blend-mode: color`.

### Studio easter egg — teraz TEŻ NA MOBILE (ZAAKCEPTOWANE „też super")
- Pulsujący hotspot → lightbox z realizacjami działał tylko na desktopie (celuje w LEWY ekran konsolety). Teraz **na mobile celuje w ŚRODKOWY ekran** (`HOT_MOBILE fx:.451` w `src/js/studio.js`, skalibrowane z podglądu tła `assets/qubatura.eu-tlo-dep-studio.webp`).
- `position: fixed` + `touch-action: pan-y` (kolumna Studio scrolluje nad stałym tłem; swipe dalej przewija). CSS: zdjęty `display:none !important` na mobile.
- Lightbox `SHOTS` w studio.js (audiobooki: Valentino Rossi / Radio Katowice).

### Performance / lekkość (07-14, HEAD 5438bf6)
- **Problem**: `build-prod.mjs` kopiował całe `assets/` → FTP wgrywał **~144 MB nieużywanych masterów** (.MOV 2×30MB, .HEIC, .jpeg, źródłowe PNG, contact-ref, screenshoty, theme.mp3) PUBLICZNIE na produkcję.
- **Fix**: filtr w build-prod — media kopiowane tylko gdy referencowane w kodzie; struktura/fonty(woff2)/svg/html zawsze; **samo-weryfikacja** przerywa build gdy używany asset by nie trafił do dist. Mastery zostają w git (backup), nie na serwerze.
- **Wynik**: deploy **157 MB → 13 MB**; FTP skasował mastery z serwera; zweryfikowane curl: używane=200, mastery=404; strona serwuje identycznie.
- Krytyczny load home był już lekki (webp ~150KB, wideo 274KB, audio `preload='none'`+on-gesture, Three/GSAP z CDN).

## 🎯 Do zrobienia dalej (Kuba wróci po /clear)

### 1. Audyt mobile — z Kubą (telefon) — NAJWIĘKSZY zwrot pod Awwwards
Jury testuje na telefonie. Przejść całą stronę na iPhonie: touch-targety ≥44px, zero poziomego scrolla, galeria Events (wtopienie na mobile bez hovera), Studio egg (czy puls trafia w środkowy monitor + scroll), colophon (historia dryfu iOS — patrz pamięć `qubatura-ios-backdrop-filter`), formularz. **Weryfikacja on-device przez Kubę — headless NIE odtwarza iOS.**

### 2. EN — pełna wersja angielska (i18n OD ZERA, osobna sesja, kilka godzin)
Dziś przełącznik `#lang-toggle` to **ATRAPA** (zmienia tylko literkę EN↔PL i `lang=`, nic nie tłumaczy — w inline-script w `src/index.html` ~linia 763). Treść siedzi w `index.html` (~233 elementy) **PLUS zaszyta po polsku w modułach JS** (HUD nawigacji w `navigation.js`, konsola kontaktu `contact-console.js`, colophon, `lab.js`, `pong.js`, `studio.js` SHOTS). Trzeba: mechanizm podmiany (data-pl/data-en lub słownik i18n) + dobre marketingowe tłumaczenie + test obu języków (EN dłuższy → ryzyko przełamań w nav/HUD/guzikach). NIE robić na szybko (słabe tłumaczenie szkodzi Content).

### 3. Zdjęcia / materiały (Kuba dorzuci)
- **Wsady do Studio** — Kuba przygotowuje (materiały do playerów / lightboxa Studio).
- Możliwe nowe fotki Events / materiały. Zdjęcia Events: `assets/events/ev-XX.webp` (lista w `gallery.js` MEDIA). Nowe → dodać webp + wpis w MEDIA.

### 4. a11y — dokończenie (solo, niskie ryzyko)
alt/aria/kontrast WCAG/skip-link (część focus już jest z Batch A).

### 5. Rekomendacja audio (decyzja Kuby)
`assets/audio/pulse.mp3` (3.97MB) i `main.mp3` (2.49MB) ciężkie jak na web-loop. Ładują się leniwie (nie krytyczne), ale gdyby Kuba podrzucił lżejsze wersje (niższy bitrate/mono) — waga spadnie mocno. To jego działka (inżynier dźwięku) — nie ruszać bez niego.

## 📁 Kluczowe pliki
- `src/css/main.css` — wszystko CSS (galeria `.page-gallery`/`.gal-*`, Studio `.studio-hotspot`, typografia, podstrony).
- `src/js/gallery.js` — galeria Events (jedno `<img.gal-photo>`, lightbox, MEDIA).
- `src/js/studio.js` — Studio easter egg (HOT desktop / HOT_MOBILE, lightbox SHOTS).
- `src/index.html` — treść + inline lang-toggle (atrapa).
- `tools/build-prod.mjs` — build dist/ z filtrem masterów + samo-weryfikacja.
- `tools/stamp-version.mjs` — cache-bust przed pushem.
- `.github/workflows/deploy.yml` — FTP deploy (SamKirkland/FTP-Deploy-Action, kasuje pliki spoza dist).

## ⚙️ Konwencje
- Deploy: edit → `node tools/stamp-version.mjs` (gdy zmiana src) → `git push origin dev` → live ~1.5min.
- Show-before-commit dla ryzykownych; typografia/galeria = Kuba ocenia na żywo (był w pętli feedbacku na Macu/incognito).
- iOS: `dvh`/`svh` nie `vh` dla wysokości overlayi.
- Formularz `src/send.php` (PHP7.4). `.htaccess` w `src/` → build → root (HTML no-cache, assety immutable).
- Odpowiedzi po polsku, vanilla JS, dark, premium (zero generycznych AI aesthetics).

## ⚠️ Lekcje warsztatowe (tej sesji)
1. **Headless Chrome padł w połowie sesji** — produkował 0-bajtowe PNG mimo EXIT 0 (nawet znany-dobry poligon). Nie dało się weryfikować wizualnie → deploy-i-Kuba-ocenia był jedyną drogą. `?query` psuje `file://`. `--virtual-time-budget`/`--default-background-color` też potrafiły zablokować zrzut. Poligon+screenshot działał TYLKO na początku sesji.
2. **Scena już fioletowa** → nie dodawać fioletu do fioletu; integracja przez wychłodzenie/desaturację treści.
3. **Deploy wysyłał mastery** — pilnować, żeby ciężkie źródła szły do `_source/` (gitignore) albo były filtrowane przez build.
4. Bash tool ≠ PowerShell — commit message here-stringiem `<<'EOF'`/`$(printf …)`, NIE `@'…'@` (raz zepsuło subject commita `@ …`).

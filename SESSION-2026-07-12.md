# Session handoff — 2026-07-12 (qubatura.eu) — CZYTAĆ PIERWSZE po /clear

Kontynuacja po SESSION-2026-07-11.md. **STRONA LIVE https://qubatura.eu.** Auto-deploy: `git push` dev → GitHub Action → FTP → ~1.5 min. **`node tools/stamp-version.mjs` PRZED każdym pushem** (inaczej iOS nie pobierze nowych modułów). HEAD na koniec sesji: `eae1c38`. Repo: `C:\Users\biuro\OneDrive\Pulpit\Claude\qubatura.eu`, branch `dev`. Dwa luźne screeny `synget desktop.png` / `synget mobile .PNG` są untracked — NIE commitować (materiał referencyjny Kuby).

## ✅ ZROBIONE I LIVE w tej sesji (07-12)
1. **Galeria Events na MOBILE — „Iluminator"** (ZAAKCEPTOWANA przez Kubę: „super"). Okno-apparition w flow kolumny Events pod CTA, aspect 4/3, autoplay+kropki, ⊕ i strzałki widoczne na stałe (brak hovera), tap→lightbox. Reużywa istniejący silnik. Zdjęty globalny kill `.page-gallery display:none` na mobile; `overflow-x:hidden` na kolumnie Events.
2. **Sygnet mobile — ZAMKNIĘTY** („szots", Kuba zamknął, NIE odgrzebywać — patrz pamięć `qubatura-signet-mobile-substancja`). Finalny look „czyste szkło + lawa primary + okno na froncie": mobile `uFillDensity 0.28`, `uOpal 2.4`, `uFrontFlat 0`, `uFrontClear 1` (smoothstep 0,0.22 fresnel). Desktop nietknięty.
3. **Colophon iOS — dryf „po 1-2s w prawo i w dół" NAPRAWIONY.** Przyczyna (wskazówka Kuby: ładuje się dobrze, ucieka po chwili): overlay przypinany do `visualViewport` na KAŻDY resize/scroll → iOS po otwarciu chowa pasek adresu → zdarzenie → syncVV nadpisywał left/top niezerowymi offsetami → skok. Fix w `colophon.js`: przypięcie TYLKO RAZ przy open (bez żywych listenerów), offset tylko przy realnym pinch (scale>1). ⏳ Kuba weryfikuje na STARSZYM iPhonie rano.
4. **Sprzątnięty martwy panel `?diag`** z colophon.js (showDiag/hideDiag/dbg/DIAG).

## 🎯 ZLECENIE NA RANO — „Lista designerska pod Awwwards" (Kuba autoryzował pracę autonomiczną z uprawnieniami)
Notatka designera (QUBATURA LAB) + moje dodatki. Kuba: „ufam ci, zrób do rana". **Zakres = BATCH A + moje niskie-ryzyko. #5 (zdjęcia) WSTRZYMANE do jego decyzji.**

### ⚠️ KLUCZOWA korekta do notatki
Designer pisze „główny font Outfit" i „dodać monospace" — BŁĄD: strona używa **Chakra Petch** (display) + **Space Mono JUŻ wpięty** jako `--font-mono` (main.css:18-19). Więc NIE dodawać nowego fontu (strona ma być lekka — [[qubatura-lekkosc-priorytet]]) — tylko **rozszerzyć Space Mono** tam gdzie go nie ma.

### BATCH A — „Lab Typography Pass" (CSS only, niskie ryzyko, 1 deploy)
- [ ] **#1 Mono na technicznych listach** — `--font-mono` (Space Mono) na `.page-services` (oferta Events/Studio/Lab) + listach parametrów/sprzętu które dziś lecą display. ⚠️ NIE na akapitach opisowych (`.page-lead`) — mono na całych zdaniach zabija czytelność (jury karze). Opisy zostają Chakra Petch.
- [ ] **#2 Tagi tech → chip terminalowy** — `text-transform:uppercase` + `letter-spacing:0.1em` + −1/2px rozmiaru na tagach (`.cph-stack span`, tagi Lab `.pc-*`/lab-tag jeśli są). Sprawdzić czy „Three.js"→„THREE.JS" nie zgrzyta.
- [ ] **#3 Line-height +10-15%** w ciemnych boksach opisowych (`.page-lead` i pokrewne). Czysty zysk czytelności (Usability).
- [ ] **#4 Bullety → techniczny marker** — okrągłe kropki w `.page-services` → ostry fioletowy kwadrat `▪` lub `>` przez `::before` (NIE jako treść — accessibility).

### BATCH A+ — moje dodatki (niskie ryzyko, pod jury; robić razem z A)
- [ ] `::selection` w kolorze marki (violet `#6B2FD9` tło / off-white tekst) — premium detal, trywialny.
- [ ] `:focus-visible` — czyste stany focusu (brand outline) na interaktywnych: `.nav-item`, guziki `.page-cta`/`.cph-close`/gallery, linki. Jury sprawdza dostępność klawiaturą.
- [ ] Spójność „kickerów"/labeli sekcji (mono, uppercase, spacing) — kohezja z #1/#2.
- [ ] (opcjonalnie) subtelny custom scrollbar na przewijanych kolumnach mobile.

### BATCH B — WSTRZYMANE (NIE robić autonomicznie)
- [ ] **#5 Wtopienie realnych zdjęć (Events)** — diagnoza słuszna ALE: galeria „światło z ciemności" JUŻ wtapia zdjęcia (maska-fade owalna, duch+glow, bez prostokąta) — designer mógł widzieć starszą wersję → NAJPIERW pokazać Kubie/designerowi aktualną. Glitch/przetarcie = RYZYKO „AI aesthetic", jury karze; realne realizacje = dowód wiarygodności, nie chować. Osobny, ostrożny temat PO decyzji Kuby.

### ATUTY — NIE RUSZAĆ (eksponować pod jury)
Tech-flex „O tej stronie" (colophon), imersja/art direction, struktura 3 departamentów z sygnetu, Q-PONG. Sygnet — ZAMROŻONY.

## ⚙️ Zasady wykonania autonomicznego (rano/nocą)
- Wszystko CSS-owe, głównie `src/css/main.css`. `stamp-version.mjs` przed pushem. Push dev = deploy live ~1.5min.
- **Weryfikacja:** desktop mogę sprawdzić (headless/lokalnie `python -m http.server 8080` → localhost:8080/src/), ALE **mobile/iOS weryfikuje Kuba rano** (headless NIE odtwarza iOS — [[qubatura-etap7-stan]]). Typografia to niskie ryzyko layoutu.
- **Zostawić rano RAPORT**: co zmienione (per punkt) + co konkretnie sprawdzić na iPhonie.
- **NIE ruszać:** 3D/scena/sygnet (zamrożony), tło-wideo (NIE kasować), #5, żadnych dużych zmian layoutu. Trzymać premium, ZERO generycznych AI aesthetics.
- Konwencje: PL, vanilla, dark, show-before-commit (dla ryzykownych — tu batch A niski, można lecieć), iOS: `dvh`/`svh` nie `vh`.

## Konwencje ogólne
Deploy: edit → `stamp-version.mjs` → push dev → live ~1.5min. Formularz = `src/send.php` (PHP7.4). `.htaccess` w `src/` → build-prod → root (HTML no-cache, assety immutable).

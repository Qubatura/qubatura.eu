# Session handoff — 2026-07-02 (qubatura.eu)

Notatka dla następnego czatu Claude + lista prac. Kontynuujemy, gdy Kuba wróci.

## Stan repo
- **Branch:** `dev` (JEDYNY branch; GitHub Pages serwuje `dev` → **live: `https://qubatura.github.io/qubatura.eu/src/`**)
- **HEAD:** `1e5b85a` (wszystko zacommitowane i wypchnięte)
- **Serwer lokalny (MSI):** z roota projektu `python -m http.server 8080` → `http://localhost:8080/src/`
- **Stack:** Three.js r0.165 (CDN importmap), vanilla JS modules, GSAP (CDN), bez bundlera.

## ⚠️ CACHE — czytać zawsze
iOS Safari (i Chrome) cache'ują moduły ES **i CSS per-URL** — samo `?v=` na adresie strony NIE wystarcza.
- **Rozwiązane:** `tools/stamp-version.mjs` stempluje wspólny token `?v=` do wszystkich względnych importów JS + `<script main.js>` + `<link main.css>`. **URUCHAMIAĆ PRZED KAŻDYM push:** `node tools/stamp-version.mjs` (token = czas base36; idempotentne; CDN 'three'/'gsap' nietknięte). Aktualny token: `20260702f`.
- **Kuba testuje na iPhone:** gdy „nie widać zmian" → Ustawienia → Safari → Wyczyść historię i dane witryn (albo karta Prywatna). NIE wierzyć w „nie zrobione" bez tego — weryfikować `curl` na żywo.
- iOS parallax/żyroskop testować TYLKO na HTTPS (Pages), nie localhost.

## Zrobione dziś (2026-07-02)
1. **Sygnet hover-obrót (A2):** idle-sway (±0.51 rad) przebijał zwrot → `idleDamp` w signet.js (tłumi idle przy hoverze) + nudge 0.55/0.40 (navigation.js). Pewny zwrot ku działowi.
2. **Sygnet płyn (A1):** gęstszy kolor (opal jasny koniec 0.26,0.16,1.0; `uFillDensity` 0.82); sygnet -7wu niżej (desktop).
3. **Sygnet ogólnie:** `uBodyDim` 0.90→0.84 (mniej intensywny); wybuch glow na hover 1→0.55 (stan ustalony 0.3 bez zmian); zwrot dostojny — startNudge 1.1s power2.inOut.
4. **Sygnet MOBILE — mat naprawiony:** przywrócone struktury szkła TYLKO mobile (`refractionStrength` 0.13, `uGlassFloor` 0.28) — kolor zostaje 1:1 (zimny), więc szkło wraca bez mleczności. `const isMobile` w signet.js.
5. **Studio scroll mobile (B2):** playery odsłonięte w przewijanej kolumnie (były `display:none`), hero skompaktowany, cue „▽", grid 1-kol.
6. **Powrót Kontaktu:** z dolnego „← Powrót" na logo+strzałkę w lewym górnym rogu (jak podstrony), kolor NEUTRALNY biało-szary. Mobile pod safe-area.
7. **Mobile — nazwa działu:** zawija się POD logo+strzałkę, wyrównana do startu hero (6vw, Opcja 1).
8. **Galeria Events:** `gallery.js` + `#lightbox`. W ramce: slideshow DUOTONE (grayscale+fiolet event mix-blend) → pełny kolor na hover, autoplay 4.5s, licznik „01/06" + klikalne kropki. Klik → lightbox prawie pełnoekranowy: strzałki ‹ ›, klawiatura ←/→/Esc, klik-tło zamyka, autoplay 5s, opcjonalny podpis. Placeholder „FOTO 0N" gdy brak pliku. Mobile jak Studio.
9. **WYDAJNOŚĆ teł (priorytet: lekkość):** PNG→WebP (ffmpeg). `planet-bg` 18,8MB→222KB; 3× tło-dep ~4MB→~150KB. Referencje `.png`→`.webp` (scene.js+CSS). Tła departamentów doładowane jako śledzone Promise w `initLoader` → 100% czeka na nie + są w cache przed nawigacją (koniec „przeskoku tła"). Usunięte stare PNG (-30,5MB).

## Pokrętła sygnetu (signet.js uniforms)
- Desktop: `uBodyDim` 0.84, `uFillDensity` 0.82, opal jasny (0.26,0.16,1.0), `uPrimaryShift` 0.90, `uFrontGlass` 0.85, `uInnerDepth` 2.2, `depth` 7.5.
- Mobile (osobno TYLKO struktury szkła): `refractionStrength` 0.13, `uGlassFloor` 0.28. Reszta koloru = 1:1 z desktopem.
- Ruch: nudge amp 0.55/0.40 (pong 0.40), startNudge 1.1s power2.inOut, idleDamp = 1 - intensity*0.6, beat glow peak 0.55.

## DO ZROBIENIA — kolejka pod PREMIERĘ (cel: dziś/2026-07-02)
Zakres wg Kuby (pamięć: qubatura-premiera-zakres):
1. **Events — CTA/guzik:** teraz „Porozmawiajmy o evencie →" (otwiera Kontakt). Kuba chce **„Zaprojektuj event →"** i/lub placeholder pod **konfigurator** z dopiskiem **„w budowie"**. + wrzucić zdjęcia do `assets/events/ev-01.jpg … ev-06.jpg` (tablica `PHOTOS` w gallery.js — liczba/podpisy edytowalne).
2. **LAB — treść (system kafelek `.pl-card`):**
   - „**Oprogramowanie audio**" — kafelek z playerem + **osobna podstrona** opisująca (wrzuci się **trailer** + kilka pozycji).
   - „**Strony www**" — kafelek + kilka **linków do naszych prac**.
   - kilka innych kafelków.
3. **Formularz** — zbudować JEDEN wzorzec (reszta na jego bazie).
4. **Anti-bot** (honeypot/prosty challenge).
5. **Cookie consent** — minimalny (niezbędne ciasteczka).
6. **SEO:** zmienić `<title>` (Kubie się nie podoba) + **brakuje `assets/og-image.png`** (og:image wskazuje na nieistniejący plik → brak podglądu linka).

## Czeka na Kubę (feedback / materiały)
- Ocena po wyczyszczeniu cache: **galeria Events** (interakcje/duotone), **sygnet mobile** (mat→szkło? B1), 3 poprawki desktop (jasność/glow/zwrot), powrót Kontaktu, wyrównanie nazwy działu.
- **Zdjęcia Events** (Kuba przygotowuje), treść/linki Lab, próbki głosów (przyszła sekcja „Posłuchaj głosów Qubatury" w Studio — backlog).
- „Player na home" — drobne modyfikacje (Kuba doprecyzuje; to polish, robić PO feedbacku home/sygnet).
- Osobna rozmowa o **optymalizacji** (Kuba zapowiedział). Loop-wideo tła = przyszły test (osobny plik, NIE kasować).

## Konwencje/pamięć
- Odpowiedzi po polsku. Vanilla JS, dark theme, premium feel, bez generycznych AI aesthetics.
- Pokazuj lokalnie na MSI; Mac/iPhone widzą tylko po push (Pages).
- Pamięć projektu: `qubatura-etap7-stan`, `qubatura-premiera-zakres`, `qubatura-lekkosc-priorytet`, `qubatura-iteration-workflow`.

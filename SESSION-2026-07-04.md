# Session handoff — 2026-07-04 (qubatura.eu)

Kontynuacja po 2026-07-03. Dzień: dostrojenie mini-playera, przebudowa galerii Events („światło z ciemności"), SEO. Wszystko commit+push na `dev`.

## ⏭️ NA START (po /clear) — pierwsze do zrobienia
Kolejka pod premierę, po kolei (ustalone z Kubą):
1. **Events CTA** → „Zaprojektuj event →" + placeholder konfiguratora „w budowie".
2. **Lab — treść:** Event Player (trailer+opis w `#product-overlay`), kafelek „Strony www" + linki; ew. kolejne kafelki.
3. **Formularz** — jeden wzorzec (reszta na jego bazie).
4. **Anti-bot** (honeypot) — dopięcie do formularza.
5. **Cookie consent** — minimalny.
6. ~~SEO~~ ✅ ZROBIONE dziś (niżej).

**0. SZYBKI FIX NA START (Kuba, przed pytaniami):** góra galerii Events wisi WYŻEJ niż start hero („PRODUKCJA…") — zrównać **górną krawędź kadru do linii startu hero** (symetria/elegancja; pod galerią lecą tylko wpisy klientów). UWAGA techniczna: hero `#page` jest **flex `align-items:center`** (wyśrodkowany pionowo), więc góra tytułu nie jest stałą `vh` — dobrać `top` galerii (teraz `top:15vh` w `.page-gallery`) wizualnie na żywo z Kubą, albo policzyć od wysokości hero.

## Decyzje/kierunki — pytania Kuby (2026-07-04, przed clear)

### Q1 — hosting i modyfikacje po wdrożeniu / sprzężenie z GitHub
- Strona jest **statyczna** (HTML/JS/CSS, ZERO backendu) → hosting trywialny i wymienny; już jest na GitHub + Pages. Modyfikacja później = edycja plików → commit → push → live ~1 min (dokładanie/wymiana zdjęć, tekstów, sekcji = zwykłe edycje plików).
- „Sprzężenie z GitHub żebym miał dostęp" = **git JEST tym mostem.** Pracuję przez Claude Code na maszynie Kuby na lokalnym repo; gdy odpala mnie w folderze repo — edytuję, commituję, pushuję. NIE sięgam samodzielnie na serwer; dostęp = repo.
- Jeśli Kuba przeniesie na WŁASNY serwer: git zostaje źródłem prawdy, dokładamy auto-deploy (GitHub Actions → FTP/SSH na jego host) przy każdym push. Workflow bez zmian.
- **Rekomendacja:** zostać przy git; docelowo rozważyć **Cloudflare Pages/Netlify** (darmowe, custom domain qubatura.eu, git-connected, DAJĄ serverless funkcje — przydadzą się do formularza w Q2). Jedna decyzja spina Q1+Q2.

### Q2 — formularz kontaktowy (główny) + tagowanie w Spark
- Forma: **konwersacyjna, przystępny język.** Krok 1 = wybór „toru" (Event / Produkcja studyjna / Web-design / Platforma-software) prostym językiem → potem: pomysł → czy ma koncepcję → budżet → kontakt. Lejek wyciągający kluczowe info. UI zbuduję (vanilla JS, styl strony); **honeypot anti-bot wbudowany = zamyka też #4**.
- Mail na JEDEN adres **biuro@qubatura.eu** (bez dzielenia na departamenty). Tagowanie przez **PREFIKS TEMATU**: `[EVENTS] …` / `[STUDIO] …` / `[LAB] …` / `[WEB] …` wg wybranego toru → w Spark reguła/etykieta per prefiks = od razu widać kategorię. Zero osobnych skrzynek.
- Technika (statyk sam nie wyśle maila): **(a)** serwis formularzy (Web3Forms/Formspree — darmowe, EU-ok): POST → mail na biuro@ z naszym własnym tematem-tagiem; działa NAWET na GitHub Pages już teraz (Kuba: ~2 min rejestracji po access key). **(b)** serverless (Cloudflare/Netlify) + email API (Resend) — więcej kontroli, wymaga zejścia z Pages.
- **Rekomendacja na premierę: (a)** — szybkie, pewne, zero backendu; później ew. upgrade do (b). Spina się z Q1 (jak chcemy serverless → Cloudflare Pages).
- Szczegóły/implementacja: PO clear.

## Stan repo
- **Branch:** `dev` (jedyny; Pages serwuje `dev`). **Live:** `https://qubatura.github.io/qubatura.eu/src/`.
- **HEAD:** `7c7b917` (wszystko zacommitowane i wypchnięte, working tree czysty).
- **Serwer lokalny (MSI):** z roota `python -m http.server 8080` → `http://localhost:8080/src/`.
- **Stack:** Three.js r0.165 (CDN importmap), vanilla JS modules, GSAP (CDN), bez bundlera.

## ⚠️ CACHE — przed KAŻDYM push zmieniającym JS/CSS
`node tools/stamp-version.mjs` — stempluje wspólny token `?v=` do importów JS + `<link main.css>`. iOS/Pages NIE bustuje modułów ES samym `?v=` na adresie strony. Lokalnie do modułów ES: DevTools → Network → **Disable cache**.

## Zrobione dziś (2026-07-04) — chronologicznie

### 1. Mini player (home) — dostrojenie (commit 5b018db)
- Kuba: waga za duża (równa KONTAKT/EN) + fiolet w rogu ma zniknąć („ma być czysto").
- `main.css`: `.mp-cur` 15px/bold/primary → **11px / waga 500 / biel 0.5** (bez fioletu); `.mp-side` 9px/biel 0.17; `.mp-arrow` 14px/biel 0.25; `.mp-title` 9px/biel 0.3. Player jest teraz **podrzędny** wobec headera.
- **Struktura:** `#top-right` + `#music-player` opakowane w `#top-cluster` (flex-kolumna, `align-items:flex-start`, `gap:11px`) → strzałka `‹` playera ląduje **pod „KONTAKT"** (wspólna lewa krawędź), niezależnie od długości tytułu. Mobile: pozycję przeniesiono na `#top-cluster` (`top:16/right:16` + notch-safe).

### 2. Galeria Events — nowy język „ŚWIATŁO Z CIEMNOŚCI" (commit 5b018db)
Kuba: ostra ramka od początku nie leżała; galeria wyglądała jak przyklejone okno. Przeszliśmy przez poligon (osobny HTML z wariantami + headless-screenshot — patrz pamięć `qubatura-poligon-screenshot`). Wybrany wariant **G**.
- **Bez bordera / tła / box-shadow.** Owalna **maska-fade** na `.gal-stage` (cały obwód rozpływa się, próg `50%`) → brak prostokąta.
- **Zdjęcie = dwie warstwy tego samego pliku** (jedno pobranie, zmiana w `gallery.js`): `.gal-ghost` (przygaszona kopia, opacity `.32` — STRUKTURA) + `.gal-glow` (`mix-blend-mode: screen`, contrast 1.16/brightness 1.05 — czernie znikają, jasne partie ŚWIECĄ, scena prześwituje przez cienie = zdjęcie „oddycha").
- **Fioletowa łuna Events:** `.gal-stage::after` (screen, `45%`, gaśnie na hover) + basen `.page-gallery::before` (`30%`). To NIE stary duotone (tamten tintował cienie i znikał) — tu barwimy tylko światło.
- **Hover** = sygnał kondensuje się w realne, KOLOROWE zdjęcie (`.gal-ghost` → opacity .95 + grayscale 0 + scale; `.gal-glow` cofa się) + kątowniki celownika (tr+bl) + **⊕** (zamiast napisu „Kliknij…").
- **Autoplay ZAWSZE** — zdjęto blokadę `prefers-reduced-motion` z `armFrame`/`startLbAuto` (pokaz slajdów to treść, nie ozdoba; ma działać niezależnie od ustawień systemu usera — patrz pamięć `qubatura-generalne-dzialanie`). Autoplay pauzuje tylko na hover (`is-hot`) i przy otwartym lightboxie.
- **Pokrętła (main.css):** fade `.gal-stage` mask `50%` · fiolet-łuna `.gal-stage::after` `45%` (0% = czyste B&W / wariant „F") · basen `::before` `30%` · duch `.gal-ghost opacity .32` · świecenie `.gal-glow brightness 1.05`.

### 3. SEO (commit 7c7b917)
- **`<title>`** „Qubatura Group — Events · Studio · Lab" (poprzedni Kubie się nie podobał). *Do ewentualnego zawetowania — alternatywy: „Qubatura — Przybywamy z sygnałem" (brand) / „Qubatura Group | Nagłośnienie, produkcja audio, druk 3D — Sosnowiec" (keyword).*
- **meta description** keyword-rich; **canonical** `https://qubatura.eu/`.
- **Open Graph** rozszerzony: `site_name`, `locale pl_PL`, `og:title` „Przybywamy z sygnałem", `og:image` + width/height/alt.
- **Twitter** `summary_large_image` (title/description/image).
- **`assets/og-image.png`** — brandowa karta 1200×630 (wordmark QUBATURA z `logo-lockup.svg` jako maska → off-white, łuna fioletu, `EVENTS · STUDIO · LAB`, tagline „Przybywamy z sygnałem", `qubatura.eu`). Wygenerowana headless-Chrome.
- ⚠️ **DO POTWIERDZENIA przez Kubę:** OG/canonical używają absolutnego `https://qubatura.eu/…`. Jeśli domena `qubatura.eu` NIE jest jeszcze podpięta do Pages (live to `qubatura.github.io/qubatura.eu/src/`), to `og:image` się nie załaduje w podglądach social. Sprawdzić czy custom domain działa; jak nie — trzeba zdecydować domenę/ścieżki.

## Czeka na Kubę (materiały / feedback)
- **Player:** pliki mp3 + tytuły (1 wyraz EN) do `assets/audio/ambient-01..03.mp3`; ocena na Macu.
- **Events:** paczka zdjęć (więcej / wybór najlepszych) + testimoniale pod galerię + docelowa kolejność (`MEDIA` w `gallery.js`, teraz shuffle).
- **SEO:** akceptacja `<title>` + potwierdzenie domeny (wyżej).

## Backlog / przyszłość (po premierze)
- **Podstrony pod portfolio / Awwwards** — pomysł Kuby (kilka podstron, żeby było konkurencyjnie i można zgłosić na award). Parkujemy, Kuba myśli nad zakresem.
- Magenta „gra" w playerze (hook `data-playing` jest), próbki lektorów w Studio, osobna rozmowa o optymalizacji ([[qubatura-lekkosc-priorytet]]).

## Konwencje
- Odpowiedzi po polsku. Vanilla JS, dark theme, premium feel, bez generycznych AI aesthetics.
- Pokazuj lokalnie na MSI; Mac/iPhone po push (Pages) + `stamp-version.mjs` przed pushem JS/CSS.
- Show-before-commit; Kuba często daje zgodę na porcję → commit+push, ocenia na Macu.
- Trudne decyzje wizualne → poligon + headless-screenshot (`qubatura-poligon-screenshot`).

# Session handoff — 2026-07-06 (qubatura.eu) — przed /clear

Wielka sesja 07-05 → 07-06. Ostatnia prosta przed premierą. **Wszystko na `dev`, NIC niezacommitowane** (working tree dirty). Ten plik = pełny stan pod restart kontekstu (po `/clear` czytać JAKO PIERWSZE, plus pamięć + repo).

## Stan repo
- **Branch:** `dev`. **HEAD:** `88d627a` (nic dziś nie zacommitowane). **Serwer:** `python -m http.server 8080` z roota → `http://localhost:8080/src/`.
- **Token cache:** `mr6t2en6`. ⚠️ **przed pushem:** `node tools/stamp-version.mjs` — dodane NOWE moduły: `cookie-consent.js`, zmiany w `contact-console.js`, `player.js`, `lab.js`. Potwierdzić że stamp je stempluje (nowy import cookie-consent w main.js).
- Poligony pomocnicze w `poligon/`: `wskaznik-obrecz.html`, `favicon.html`, `lab-produkt.html` (podglądy do headless-screenshot).

## ⚙️ KLUCZOWE DECYZJE / KONTEKST (bez tego po clearze zgubimy sens)
- **HOSTING = WŁASNY SERWER Kuby na IQ Host** (PHP potwierdzone). Domena **qubatura.eu już tam wisi** (obecnie strona „w budowie"). Plan: **wgrać stronę na jego serwer (FTP) + formularz przez skrypt PHP** (mail na biuro@qubatura.eu). **Web3Forms i Cloudflare ODPADAJĄ** — dane zostają na jego serwerze (EU, czysto prawnie). Kuba da dostęp/dane. (Web3Forms scaffold jest w kodzie, ale przełączymy send() na PHP.)
- **WIZJA KARTY PRODUKTU (Kuba, do zrobienia):** karta Event Playera ma być **zwartą nakładką ~3/4 ekranu** (jak Kontakt, ale mniejsza/kompaktowa), otwieraną z kafla — **styl jak strona programu / sklep z softem**: opis + **parametry/specyfikacja** po zawodowemu, atmosfera przyjazna i branżowa. TERAZ jest full-screen `#product-overlay` — **przerobić na zwartą kartę-modal + sekcję specyfikacji**. Wygląd dopracowujemy z Kubą (może poda referencję).
- **Branding:** Q NIE występuje fizycznie w nazwach — organicznie **Events / Studio / Lab** (nasuwa się z sygnetu). Pełna forma „Qubatura X". „Qlab" tylko luźny skrót w mowie. „Studio Q" wycofane.
- **Awwwards (cel Kuby, faza 2 po premierze):** EN i18n (jest `#lang-toggle` w nav), wydajność/lekkość, mikro-interakcje. Decyzje trzymać MODULARNIE. Nie blokuje premiery.
- **Muzyka:** start w ciszy (NIE autoplay — blokada przeglądarek + UX).

## Zrobione w tej sesji

### Formularz Kontakt — pełny lifting (zaakceptowane)
Szkiełka 1:1 z playerami (`#cc-gate`, `.cc-tor`, `.cc-wrap` — **ostre rogi 0px**, frost, prześwit, obwódka koloru działu). Wskaźnik toru = **oddychająca obręcz** `.cc-ring`. Nagłówek **CENTRUM ŁĄCZNOŚCI** (kicker) + **„‹ Wstecz" po lewej** + **licznik `01/03`**. Teksty: „Jaką masz sprawę?", „Wybierz temat — dalej poprowadzimy.", brama bez `◗◗◗` („otwórz centrum łączności →").

### Branding + Lab treść
Kafle/nav Events/Studio/Lab. Opis Lab: „Strony www, aplikacje, UX design, IoT, pomiary akustyczne" (druk 3D OUT, pomiary IN). Podpowiedzi Lab: www / aplikacja / UX / IoT / pomiary / luźny.

### Kontrast, muzyka, wave
KONTAKT/EN + tagline 0.45→0.62 + cień (player nietknięty). Player: **Theme / Pulse / Relax** (`assets/audio/Qubatura.eu-theme.mp3`, `-pulse.mp3`; **relax = plik dojdzie** pod nazwą `Qubatura.eu-relax.mp3`). **Puls-zaproszenie** na ikonce dźwięku: nie gra → łagodny oddech+poświata; gra → taniec equalizera (`#sound-toggle[data-active="false"]` w main.css).

### RODO + Polityka
Checkbox `.cc-rodo` (wymagany, blokuje wysyłkę) + honeypot `.cc-hp` (`botcheck`). **`src/polityka-prywatnosci.html`** — brandowa, dane KOMPLETNE: **Qubatura Jakub Hyrja, ul. Kępa 17, 41-219 Sosnowiec, NIP 6443374174**, biuro@qubatura.eu, tel 505 512 305. Linki `[data-policy]` (checkbox + stopka Kontaktu), SPA-proof.

### Cookie consent
`src/js/cookie-consent.js` (`initCookies()` w main.js) — baner „tylko niezbędne", localStorage `qub-cookie-ok`, link do polityki. Style `.cookie-bar` w main.css.

### Web3Forms scaffold (DO ZMIANY na PHP)
`WEB3FORMS_KEY` (pusty) na górze `contact-console.js`; `send()` async: honeypot+RODO → brak klucza = pokaż sukces (podgląd) → klucz = POST. **DECYZJA: przełączyć na PHP na IQ Host**, nie Web3Forms.

### SEO
`<title>` = **„Qubatura — obsługa techniczna imprez · studio · strony internetowe · Śląsk"**. `sitemap.xml` + polityka. JSON-LD: `legalName` „Qubatura Jakub Hyrja" + `vatID` „PL6443374174".

### Favicon (kierunek ZAAKCEPTOWANY, NIE wpięty)
`assets/favicon-dark.svg` — **biały sygnet na ciemnym zaokrąglonym tle**, sygnet powiększony ~1.22× (środek czytelniejszy). ⚠️ `index.html` DALEJ na starym `favicon.svg`. **DO ZROBIENIA:** wpiąć nowy + komplet (apple-touch-icon 180, PNG 32 fallback — renderować z SVG). (Kuba rozważa rebrand później — to nie teraz.)

### Karta produktu Event Player (zbudowana, DO PRZEROBIENIA na 3/4 modal)
`#product-overlay` rozwinięty: kicker/tytuł/podtytuł + **slot screena** (`.prod-shot`, screen wpięty przez `lab.js` → `asset('../assets/lab/event-player.webp')`, 110 KB) + hook + **4 funkcje** (Automix / Dżingle+spoty / Podkład z duckingiem / Sampler 9 padów) + „Dlaczego inaczej" + guziki **„Pobierz · wkrótce"** / **„Pobierz instrukcję · wkrótce"** + hint. Copy = język do techników/DJ-ów (Kuba ma zaakceptować/skorygować, m.in. linijka „22:00, sponsor chce spot na już"). **Overlay teraz full-screen (scroll) — przerobić na zwartą kartę + sekcję parametrów wg wizji Kuby.**

### Grafiki działów (dostarczone, obejrzane) — REKOMENDACJE
Pliki `assets/{events,studio,lab}/qubatura.eu-*B-1/B-2.png` (4–6 MB). Moje rekomendacje wariantów: **Events B-1** (stół monitorowy 🔥), **Studio B-1** (mic+monitory+aureola), **Lab B-1** (holo 3D+kod+narzędzia). ⚠️ **Events B-2 brak**; `labB-1.png` wpadł omyłkowo też do `assets/events/` (posprzątać). **DO ZROBIENIA:** konwersja wybranych na webp (ffmpeg — lekkość!) + wpięcie jako tła działów (obecnie tła to `qubatura.eu-tlo-dep-*`). Player screen już skonwertowany (`assets/lab/event-player.webp`).

## ⏭️ KOLEJKA (co zostało)
1. **Karta produktu → zwarta 3/4 nakładka + parametry/specyfikacja** (wizja Kuby) — omówić wygląd
2. **Tła działów** — potwierdzić warianty (rek. B-1), webp, wpiąć; posprzątać events (B-2 brak, labB-1 do usunięcia z events)
3. **Favicon** — wpiąć `favicon-dark.svg` + komplet (apple-touch + png)
4. **Formularz → PHP** na IQ Host (czeka na dostęp/dane Kuby) + honeypot już jest
5. **Deploy na IQ Host** (podmiana „w budowie") — Kuba ogarnia serwer
6. **Commit + push** (nic jeszcze niezacommitowane!) + `stamp-version.mjs` przed pushem
7. Reszta: Studio „ożywiony monitor", Events CTA „Zaprojektuj event", 3. utwór (Relax), akceptacja copy karty

## Konwencje
PL. Vanilla JS, dark, premium, bez generycznych AI aesthetics. Show-before-commit. Trudne decyzje wizualne → poligon + headless-screenshot (Chrome: świeży `--user-data-dir` za każdym razem + `--no-sandbox`; ścieżki Windows dla `--screenshot`/`--user-data-dir`; czasem trzeba powtórzyć strzał po „NOFILE"). `stamp-version.mjs` przed każdym pushem JS/CSS.

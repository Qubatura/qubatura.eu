# Session handoff — 2026-07-11 (qubatura.eu) — czytać PIERWSZE po /clear

Kontynuacja po SESSION-2026-07-07.md. **STRONA LIVE na https://qubatura.eu.** Auto-deploy: `git push` dev → GitHub Action → FTP → ~1.5 min. `node tools/stamp-version.mjs` PRZED każdym pushem.

## ✅ ZROBIONE I LIVE w tej sesji
1. **Audio** — slot 1 mini-playera = `assets/audio/Qubatura.eu-main.mp3` (title „Main", Theme schowany). **Autoplay** „od załadowania" (życzenie Kuby) w `player.js`: próba `audio.play()` od razu + fallback na 1. gest (pointerdown/keydown, poza `#sound-toggle`). ✅ Kuba potwierdził: „muza gra, robi suspens".
2. **`.htaccess`** (`src/.htaccess`, build-prod kopiuje do roota) — polityka cache: HTML `no-cache, must-revalidate`, assety `immutable`. PRZYCZYNA: serwer IQ Host (LiteSpeed) nie wysyłał żadnego Cache-Control → iOS trzymał stary HTML → deploye „nie wchodziły". Zweryfikowane: nagłówki live OK.
3. **Signet mobile** — dążenie do „glass jak desktop" (Kuba: mobile miał „białe mleko", nie widać środka). `signet.js`: `uGlassFloor 0.28→0` (koniec floor-sheenu/mleka, 1:1 desktop), `refractionStrength 0.13→0.06`, `uOpal 1.8→2.6` (mobile, żywszy środek), `uFillDensity 0.82→0.95` (mobile). SAFETY VALVE w komentarzu: gdyby za ciemno → uGlassFloor ~0.12. ⏳ Kuba: „lepszy niż był, ale mogłoby być lepiej / środka dalej mało" — do dalszego tuningu na jego iPhonie.

## ⏳ COLOPHON iOS — CANDIDATE FIX (HEAD `28222a9`), CZEKA NA POTWIERDZENIE KUBY NA IPHONE
Karta „O tej stronie" (tagline „Przybywamy z sygnałem" → `[data-colophon]`). Kuba: „ucieka w prawo + Wróć nie działa". **Saga diagnostyczna — patrz [[qubatura-ios-backdrop-filter]] (pamięć) po pełną historię.** Skrót ustaleń:
- ❌ NIE cache przeglądarki (kanarek w tytule playera się pokazał = kod dociera).
- ❌ NIE dryf poziomy — **on-device panel diagnostyczny** (getBoundingClientRect na iPhone, ih=659) pokazał: `plate L=14 R=379` (WYŚRODKOWANA), `overlay L=0 R=393`, `deClientW=393 scrollW=393` (zero h-overflow).
- ✅ PRAWDZIWA przyczyna: `wrocBtn T=-8` (NAD ekranem). `.cph-plate max-height:88vh` — `vh` iOS liczy od DUŻEGO viewportu (≈852), widać tylko ~659 → plakietka za wysoka, przyklejona do dołu (flex-end) → góra z „Wróć" ponad ekranem. „Tekst ucieka w prawo" = opis szerszy niż plakietka, ucinany z prawej.
- **FIX (mobile CSS + colophon.js):** `.cph-plate max-height: min(100%, 86dvh)` (dvh = widoczna wysokość, uwzględnia pasek Safari; niezależny od % na flex-childzie) + `overflow-y:auto` + `.cph-close position:sticky` (Wróć zawsze na górze) + twarde `max-width:100%`/`overflow-wrap` + węższy padding + `.cph-lead` 13px. Overlay dalej przypięty do `visualViewport` (colophon.js, inline left/top/width/height, listener resize/scroll). Headless @393×659: plate H=567, Wróć onScreen+klikalny, „Full custom." w całości, tekst symetryczny 33/33.
- **PANEL DIAGNOSTYCZNY bramkowany za `?diag`** (`location.search`) — realni goście NIE widzą. Kuba/my: `qubatura.eu/?diag` → otwórz kartę → różowy panel na dole z pomiarami.

### 🔜 GDY KUBA WRÓCI (za kilka h):
1. Poproś o test: `qubatura.eu/?diag` na iPhone → tap tagline → (a) czy **„Wróć" działa/zamyka**, (b) czy **tekst nie ucięty**, (c) **screen różowego panelu** (kluczowe liczby: `wroc onScreen`, `plate H`, `plate maxH/scrollH/clientH`, `lead W`).
2. Jak OK → **USUNĄĆ diagnostykę** z `colophon.js` (funkcje `showDiag/hideDiag/dbg`, `DIAG`, wywołania) + deploy czysty. Temat colophonu zamknięty.
3. Jak dalej źle → liczby z panelu powiedzą co (czy dvh capuje na iOS: `plate H` vs `clientH`; czy Wróć `onScreen`).
4. Potem: **signet dalszy tuning** (środek/glass) + **WRACAMY DO LISTY DESIGNERSKIEJ POD AWWWARDS** (Kuba przyśle notatkę — to był pierwotny cel; colophon/audio/signet to były „jego 3 uwagi wstępne").

## Konwencje
PL. Vanilla JS, dark, premium, ZERO generycznych AI aesthetics. Show-before-commit. `stamp-version.mjs` przed pushem. iOS: ZAWSZE `dvh`/`svh` nie `vh`; headless nie reprodukuje iOS layout-vs-visual viewport → on-device panel `?diag`. Deploy: edit → push → live ~1.5 min. HEAD teraz: `28222a9`.
